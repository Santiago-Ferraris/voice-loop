import VoiceLoopApp

// The app bundle's entry point. All the wiring lives in the VoiceLoopApp
// library (so it can be exercised headlessly); this is the thin shell the
// Xcode app target compiles.
runVoiceLoopApp()
